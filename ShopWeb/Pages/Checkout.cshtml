@page
@model ShopWeb.Pages.CheckoutModel
@{
    ViewData["Title"] = "Thanh toán";
}

<link rel="stylesheet" href="~/css/checkout.css" />

<div class="checkout-container">
    <h2>📦 Thông tin đặt hàng</h2>

    @if (Model.Cart == null || Model.Cart.items == null || !Model.Cart.items.Any())
    {
        <div class="empty-cart">❌ Giỏ hàng trống - không thể đặt hàng</div>
    }
    else
    {
        <form method="post">

            <!-- 🔥 THÔNG TIN NGƯỜI NHẬN -->
            <div class="form-group">
                <label>Họ và tên</label>
                <input asp-for="Order.FullName" required />
            </div>

            <div class="form-group">
                <label>Số điện thoại</label>
                <input asp-for="Order.Phone" required />
            </div>

            <div class="form-group">
                <label>Địa chỉ</label>
                <input asp-for="Order.Address" required />
            </div>

            <div class="form-group">
                <label>Ghi chú</label>
                <textarea asp-for="Order.Note"></textarea>
            </div>

            <!-- 🔥 DANH SÁCH SẢN PHẨM -->
            <h3>🛒 Sản phẩm</h3>

            <table class="cart-table">
                <thead>
                    <tr>
                        <th>Tên</th>
                        <th>SL</th>
                        <th>Giá</th>
                        <th>Tổng</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Cart.items)
                    {
                        var total = item.product.price * item.quantity;
                        <tr>
                            <td>@item.product.name</td>
                            <td>@item.quantity</td>
                            <td>@item.product.price</td>
                            <td>@total</td>
                        </tr>
                    }
                </tbody>
            </table>

            <!-- 🔥 TỔNG TIỀN -->
            <h3>💰 Tổng tiền: @Model.Cart.TotalAmount.ToString("N0") đ</h3>

            <!-- 🔥 VALIDATION -->
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="error">@Model.ErrorMessage</div>
            }

            @if (!string.IsNullOrEmpty(Model.SuccessMessage))
            {
                <div class="success">@Model.SuccessMessage</div>
            }

            <button type="submit" class="order-btn">Xác nhận đặt hàng</button>
        </form>
    }
</div>
